import styles from "./AppNav.module.css";

function AppNav() {
  return <nav className={styles.nav}>App navigation</nav>; //🎨🎨[CSS MODULES STYLE]🎨🎨we must import this in the "AppLayout.jsx" //// Also we need to import this file in the "Homepage.jsxs" 🎨🎨[CSS MODULES STYLE]🎨🎨
}

export default AppNav;
